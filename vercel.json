{
  "buildCommand": "npm install --prefix admin-web && npm run build --prefix admin-web",
  "outputDirectory": "admin-web/build",
  "framework": "create-react-app",
  "rewrites": [
    {
      "source": "/((?!api/.*).*)",
      "destination": "/index.html"
    }
  ]
}