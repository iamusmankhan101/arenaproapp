# Server Configuration
NODE_ENV=development
PORT=3001
API_VERSION=v1

# Database
MONGODB_URI=mongodb://localhost:27017/arena-pro
MONGODB_TEST_URI=mongodb://localhost:27017/arena-pro-test

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d
JWT_REFRESH_SECRET=your-refresh-token-secret
JWT_REFRESH_EXPIRE=30d

# Admin Configuration
ADMIN_JWT_SECRET=your-admin-jwt-secret-key
ADMIN_JWT_EXPIRE=24h

# Email Configuration
# You can use Gmail, Brevo (Sendinblue), SpaceMail, or any SMTP service
# 
# OPTION 1: Gmail (Recommended for testing)
# 1. Enable 2-factor authentication on your Gmail account
# 2. Generate an app password: https://myaccount.google.com/apppasswords
# 3. Use these settings:
#    SMTP_HOST=smtp.gmail.com
#    SMTP_PORT=587
#    SMTP_EMAIL=your-email@gmail.com
#    SMTP_PASSWORD=your-16-character-app-password
#
# OPTION 2: SpaceMail (Recommended for production)
# 1. Sign up at https://spacemail.com
# 2. Get your SMTP credentials from Settings
# 3. Use these settings:
#    SMTP_HOST=smtp.spacemail.com
#    SMTP_PORT=587
#    SMTP_EMAIL=your-spacemail-email@yourdomain.com
#    SMTP_PASSWORD=your-spacemail-password
#
# OPTION 3: Brevo/Sendinblue
# 1. Sign up at https://www.brevo.com (free tier: 300 emails/day)
# 2. Get your SMTP key from Settings > SMTP & API
# 3. Use these settings:
#    SMTP_HOST=smtp-relay.brevo.com
#    SMTP_PORT=587
#    SMTP_EMAIL=your-brevo-login-email@example.com
#    SMTP_PASSWORD=your-xs-smtp-key-from-brevo
#
SMTP_HOST=smtp.spacemail.com
SMTP_PORT=587
SMTP_EMAIL=your-spacemail-email@yourdomain.com
SMTP_PASSWORD=your-spacemail-password
FROM_EMAIL=no-reply@arenapro.com
FROM_NAME=Arena Pro

# File Upload (Optional)
CLOUDINARY_CLOUD_NAME=your-cloudinary-name
CLOUDINARY_API_KEY=your-cloudinary-key
CLOUDINARY_API_SECRET=your-cloudinary-secret

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS
CORS_ORIGIN=http://localhost:3000,http://localhost:19006

# Google OAuth (Optional)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret